# deps / build
node_modules/
dist/
.astro/

# planning & prototype artifacts — not part of the published portfolio
.omc/
.playwright-mcp/
.nova/
NOVA-STATE.md
design-prototype.html
gw-*.png

# 면접 준비 원문(평문) — 절대 커밋하지 않는다.
# 암호문만 src/data/interview.sealed.json 으로 커밋된다.
private/

# os / logs
.DS_Store
*.log

# verification screenshots (repo root only)
/*.png
