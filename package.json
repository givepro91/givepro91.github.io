{
  "name": "grant-works",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-disclosure.mjs --source && astro build && node scripts/check-disclosure.mjs --dist",
    "build:nogate": "astro build",
    "preview": "astro preview",
    "admin": "node scripts/admin-server.mjs",
    "interview:seal": "node scripts/seal-interview.mjs",
    "test:interview": "node --test scripts/interview.test.mjs",
    "check:disclosure": "node scripts/check-disclosure.mjs --source",
    "check:disclosure:dist": "node scripts/check-disclosure.mjs --dist",
    "check:resume": "node scripts/check-resume-pages.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.2.0",
    "@astrojs/sitemap": "^3.3.0",
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "js-yaml": "^5.1.0"
  }
}
